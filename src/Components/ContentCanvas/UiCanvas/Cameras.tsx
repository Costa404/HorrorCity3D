import FirstPersonCamera from "../Camera/FirstPersonCamera/FirstPersonCamera";

const Cameras = () => {
  return (
    <group>
      <FirstPersonCamera />
    </group>
  );
};

export default Cameras;
